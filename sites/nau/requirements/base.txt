boto3==1.43.8
django-check-seo==0.6.4
richie==3.4.0
# Pin transitive deps of richie that are otherwise left unpinned, so a
# fresh image build doesn't silently drift to a newer version with a
# breaking DB migration (djangocms-link 5.2.0 renamed a DB column and
# caused a dev outage when nau:2.2.1/ap:1.5.1 were rebuilt). These pins
# are tied to richie==3.4.0 above: revisit them whenever richie is
# upgraded, rather than assuming they can stay untouched forever.
Django==4.2.30
djangocms-link==5.1.1
unidecode==1.3.6 # required by django-check-seo
django-configurations==2.5.1
# django-formtools 2.6.1 introduced a caching regression in
# get_form_list() that breaks the CMS wizard (TypeError: wizard_user unexpected
# kwarg). Fixed upstream in richie 3.5.1 by pinning django-formtools<2.6.
# See openfun/richie@9a84bc4715fa537fc71772db4f0abeb965c0929a
django-formtools>=2.1,<2.6
# Superior versions of django-storages are not compatible with
# ManifestStaticFilesStorage
django-storages==1.14.6
dockerflow==2026.3.4
factory-boy==3.3.3
gunicorn==26.0.0
psycopg2-binary==2.9.12
sentry-sdk==2.60.0
mysqlclient==2.2.8
# Remove django-cms dep when upgrading to richie 2.25.0
# django-cms>=3.11.0,<4.0.0
# Plugins
djangocms-snippet==3.1.1
git+https://github.com/openfun/django-cms@fun-3.11.10#egg=django-cms